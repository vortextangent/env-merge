{
  "name": "@riskalyze/env-merge",
  "version": "0.1.0",
  "private": false,
  "description": "A command-line tool for merging .env.dist changes into local variants.",
  "publishConfig": {
    "registry": "http://npm.riskalyze.com"
  },
  "main": "env-merge.js",
  "repository": {
    "type": "git",
    "url": "git://github.com/vortextangent/env-merge.git"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "dotenv",
    "merge"
  ],
  "author": "Anthony daCosta",
  "license": "GPL-3.0",
  "bin": {
    "env-merge": "./env-merge.js"
  },
  "dependencies": {
    "chalk": "^2.3.0",
    "commander": "^2.12.2",
    "dotenv": "^4.0.0"
  }
}
